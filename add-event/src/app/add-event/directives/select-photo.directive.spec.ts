import { SelectPhotoDirective } from './select-photo.directive';

describe('SelectPhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectPhotoDirective();
    expect(directive).toBeTruthy();
  });
});
